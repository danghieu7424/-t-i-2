# Chạy với bản được build trong thư mục build với file .exe
# Chạy với nguồn thông qua 2 thư mục Back và Front đồng thời cài môi trường rust trước khi chạy


    - Đề tài 2: Chụp ảnh hóa đơn → Grok Vision OCR + prompt LLM phân loại tự động (nguyên liệu/lương/điện nước…) → lưu vào DB. chi phí tháng
    - Repo GitHub sạch sẽ + file .env.example + hướng dẫn deploy.

    - Tạo 1 file PDF “Hướng dẫn sử dụng 5 phút” cho từng dự án (Notion hoặc Canva).
    - Tích hợp AI (OCR, dự đoán, nhắc lịch) đạt 80% tự động.