
## Cài theo dõi file:
```Bash
cargo install cargo-watch
```
Chạy thường: `cargo watch -w src -x "run"`
Chạy với hiệu năng cao: `cargo watch -w src -x test -x "run --release"`

## chạy trực tiếp
```Bash
cargo run
```